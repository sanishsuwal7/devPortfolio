backend:
  name: git-gateway
  branch: template

media_folder: static/img
public_folder: /img

collections:
  - name: "blog"
    label: "Blog"
    folder: "content/blog"
    create: true
    slug: "{{year}}-{{month}}-{{day}}-{{slug}}"
    editor:
      preview: false
    fields:
      - { label: "Title", name: "title", widget: "string" }
      - { label: "Publish Date", name: "date", widget: "datetime" }
      - { label: "Description", name: "description", widget: "string" }
      - { label: "Body", name: "body", widget: "markdown" }
  - name: "pages"
    label: "Pages"
    files:
      - file: "src/pages/landing/index.md"
        label: "Landing Page"
        name: "landing"
        fields:
          - { label: "Title", name: "title", widget: "string" }
          - { label: "Body", name: "body", widget: "markdown" }
          - { label: "Internal", name: "internal", widget: "hidden" }
          - {
              label: Mainpitch,
              name: mainpitch,
              widget: object,
              fields:
                [
                  { label: Title, name: title, widget: string },
                  { label: Subtitle, name: subtitle, widget: text },
                  { label: Action, name: action, widget: text },
                ],
            }
          - {
              label: Mainpitch,
              name: mainpitch,
              widget: object,
              fields:
                [
                  { label: Title, name: title, widget: string },
                  { label: Subtitle, name: subtitle, widget: text },
                  { label: Action, name: action, widget: text },
                ],
            }
          - {
              label: Bio,
              name: bio,
              widget: object,
              fields:
                [
                  { label: Title, name: title, widget: string },
                  {
                    label: Features,
                    name: features,
                    widget: list,
                    fields:
                      [
                        { label: Header, name: header, widget: text },
                        { label: Body, name: body, widget: text },
                      ],
                  },
                ],
            }
          - {
              label: Projects,
              name: projects,
              widget: list,
              fields:
                [
                  { label: Title, name: title, widget: string },
                  {
                    label: Title,
                    name: title,
                    widget: list,
                    fields: [{ label: Tags, name: tags, widget: string }],
                  },
                  { label: Body, name: body, widget: string },
                  { label: Action, name: action, widget: string },
                  { label: Link, name: link, widget: string },
                ],
            }

      - file: "src/pages/about/index.md"
        label: "About"
        name: "about"
        fields:
          - { label: "Title", name: "title", widget: "string" }
          - { label: "Body", name: "body", widget: "markdown" }
          - { label: "Internal", name: "internal", widget: "hidden" }
